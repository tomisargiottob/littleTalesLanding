import type { NextConfig } from "next"

const nextConfig: NextConfig = {
  // Enable React Compiler for performance
  reactCompiler: true,

  // Enable experimental features for ISR and caching
  experimental: {
    cacheComponents: true,
  },

  // Add TypeScript ignore build errors
  typescript: {
    ignoreBuildErrors: false,
  },

  // Optimize images for SEO and performance
  images: {
    formats: ['image/webp', 'image/avif'],
    deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048, 3840],
    imageSizes: [16, 32, 48, 64, 96, 128, 256, 384],
    domains: ['localhost', 'colorbook.com'],
  },

  // Enable compression
  compress: true,

  // Redirects for SEO
  async redirects() {
    return [
      {
        source: '/home',
        destination: '/',
        permanent: true,
      },
    ]
  },

  // Headers for SEO and security
  async headers() {
    return [
      {
        source: '/(.*)',
        headers: [
          {
            key: 'X-Content-Type-Options',
            value: 'nosniff',
          },
          {
            key: 'X-Frame-Options',
            value: 'DENY',
          },
          {
            key: 'X-XSS-Protection',
            value: '1; mode=block',
          },
        ],
      },
    ]
  },
}

export default nextConfig
